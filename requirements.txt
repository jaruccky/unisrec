# Core ML
# Install the Torch build that matches your CUDA/CPU setup if this line fails:
# https://pytorch.org/get-started/locally/
torch>=2.1.0

# BERT text embeddings for UniSRec
transformers>=4.38.0

# Progress bars
tqdm>=4.66.0

# Optional: only needed for Online Retail dataset download via Kaggle CLI
kaggle>=1.6.0